function sayHello(name){
    console.log('Hello ' + name)
}

sayHello('window')
// run code by prefixing a command in the terminal with node and the calling the file name  to run it
// ex: node app.js